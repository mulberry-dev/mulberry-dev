import { sectionPageMetadata } from "@/lib/sectionMeta"

export const dynamic = "force-static"

export const metadata = sectionPageMetadata("/about")

export default function Page() {
  return null
}
