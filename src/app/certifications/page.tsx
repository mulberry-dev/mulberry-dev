import { sectionPageMetadata } from "@/lib/sectionMeta"

export const dynamic = "force-static"

export const metadata = sectionPageMetadata("/certifications")

export default function Page() {
  return null
}
