import { sectionPageMetadata } from "@/lib/sectionMeta"

export const dynamic = "force-static"

export const metadata = sectionPageMetadata("/portfolio")

export default function Page() {
  return null
}
